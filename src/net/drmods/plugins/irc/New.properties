# $Id$
#Tags: dir, path, section, date, owner, group, size, pos, files
announce=${color}15${bold}NEW${bold}${coloroff}: [${color}15${bold}${section}${bold}${coloroff}] -> ${bold}${dir}${bold} by ${bold}${owner}${bold}/${group} (${date})
usage=${color}15${bold}NEW${coloroff}: !new [section] [count]
badsection=${color}15${bold}NEW${coloroff}: ${bold}${input}${bold} is not a valid section name.
