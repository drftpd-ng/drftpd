# $Id$
transfer.complete=Transfer complete, ${bytes} in ${seconds} seconds (${speed})

help.general=\      RESCAN : sfv scans the files in the current directory.
help.rescan=SITE RESCAN [force] - sfv scans the files in the current directory.\n\     The force option makes the slave recaculate the checksums
