unmirror {
plugin org.drftpd.plugins.mirror
class Mirror
method doSITE_UNMIRROR
perms =siteop
help Unmirror path so files are kept on one slave only
help.specific ${command} <path>
input all
output source
}
