# PRE (tags: section, dir, bytes, users, groups, bw, bwavg, bwtop, bwusertop, leechtop, messuretime)
prebw.announce=${bold}[${section}]${bold} ${color}14-PREBW-${coloroff} ${dir} :: Size: ${bold}${bytes}${bold} :: Average speed: ${bold}${bwavg}${bold} :: Best: ${bold}${bwtop}${bold}\n${bold}[${section}]${bold} ${color}14-PREBW-${coloroff} ${bw}\n${leechtop}

# Format of messure/bw (tags: time, speed)
prebw.bw.format=${bold}${time}${bold}: ${speed}
# Number of chars to trim at beginning and end of bw announce
prebw.bw.separator=|

# Format of leechtop (tags: section, dir, username, files, bytes, avgspeed, topspeed)
prebw.leechtop.format=${bold}[${section}]${bold} ${color}14-PREBW-${coloroff} :: ${username} :: ${bytes} :: ${files} files :: ${avgspeed} (Peak: ${topspeed})\n
prebw.leechtop.empty=${bold}[${section}]${bold} ${color}14-PREBW-${coloroff} ${dir} :: All leechers asleep!
# Max number of users to list
prebw.leechtop.count=3